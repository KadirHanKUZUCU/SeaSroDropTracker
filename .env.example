# Yerel API
PORT=3001

# Vercel (Dashboard → Storage → Blob + Settings → CRON_SECRET)
# Kopyala: copy .env.example .env.local  sonra token yapistir
BLOB_READ_WRITE_TOKEN=vercel_blob_rw_BURAYA
# private (varsayılan) veya public — Vercel store tipine uygun
BLOB_ACCESS=private
CRON_SECRET=
